[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm run migrate:prod && node dist/services/api/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
