'use strict';
/**
 * Web stub for react-native/Libraries/Core/setUpTimers.
 *
 * The real setUpTimers replaces global.setTimeout with a version that depends
 * on the native Timing module. On web we keep the browser's native timers.
 * This stub is a no-op so the bundle includes it but nothing is overwritten.
 */
// intentionally empty
